on:
  workflow_dispatch:
  
jobs:
  test:
    runs-on: ubuntu-latest  
    steps:
      - name: Hello world action        
        env:
          super_secret: ${{ secrets.SuperSecret }}
        run: echo "Super secret ist \"$(echo ${super_secret} | sed 's/./& /g')\""
